/*
 * Store Messages
 *
 * This contains all the text for the Store component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Store';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Store component!',
  },
});
